import 'package:flutter/material.dart';

class AppColors {
  // LOGIN AND REGISTER COLORS
  static const titleColor = Color.fromRGBO(0, 0, 0, 1);
  static const primaryButtonColor = Color.fromRGBO(0, 42, 182, 0.8);
  static const buttonColor = Color.fromRGBO(205, 216, 255, 0.5);
  static const primaryButtonTextColor = Color.fromRGBO(255, 255, 255, 1);
  static const buttonTextColor = Color.fromRGBO(30, 66, 197, 1);
  static const hintColor = Color.fromRGBO(0, 0, 0, 0.5);
  static const dividerColor = Color.fromRGBO(0, 0, 0, 0.25);
  static const dragColor = Color.fromRGBO(217, 217, 217, 1);
  static const dragColor1 = Color.fromRGBO(181, 181, 181, 1);
  static const lightBackgroundColor = Color.fromRGBO(226, 238, 255, 1);
  static const backgroundColor = Color.fromRGBO(171, 195, 222, 1);
  static const linkLowColor = Color.fromRGBO(28, 39, 77, 1);
  static const linkColor = Color.fromRGBO(0, 43, 174, 1);
  static const orColor = Color.fromRGBO(0, 0, 0, 0.25);
  static const iconBackground = Color.fromRGBO(227, 227, 227, 1);
  // HOME COLORS
  static const homeBackgroundColor = Color.fromRGBO(245, 245, 245, 1);
  static const docHomeBackgroundColor = Color.fromRGBO(221, 221, 221, 0.48);
  static const homeMainColor = Color.fromRGBO(28, 39, 77, 1);
  // EXPLORE COLORS
  static const categoryTextColor = Color.fromRGBO(100, 100, 100, 1);
  static const categoryBackgroundColor = Color.fromRGBO(255, 255, 255, 0.2);
  static const categoryBorderColor = Color.fromRGBO(100, 100, 100, 0.46);
  // APPOINTMENTS COLORS
  static const hourAvailableColor = Color.fromRGBO(196, 207, 217, 1);
  static const alseAvailableColor = Color.fromRGBO(251, 251, 251, 0.61);
  static const hourNotAvailableColor = Color.fromRGBO(196, 207, 217, 0.5);
  static const arrowBackgroundColor = Color.fromRGBO(223, 223, 223, 1);
  // FILTER COLORS
  static const removeColor = Color.fromRGBO(169, 0, 3, 1);
  static const messageBackColor = Color.fromRGBO(237, 242, 246, 1);
  // CONSULT COLORS
  static const pinColor = Color.fromRGBO(33, 43, 81, 0.4);
  // CALL COLORS
  static const someRedColor = Color.fromRGBO(219, 0, 0, 1);
  static const nonColor = Colors.transparent;
  // SETTINGS COLORS
  static const backBackColor = Color.fromRGBO(225, 225, 225, 0.62);
  static const settingTitleColor = Color.fromRGBO(66, 66, 66, 0.58);
  static const hintSettingColors = Color.fromRGBO(70, 70, 70, 0.6);
  static const logSettingColor = Color.fromRGBO(195, 24, 24, 1);
  // PAYMENT COLORS
  static const paymentBackColor1 = Color.fromRGBO(132, 98, 180, 0.37);
  static const paymentBackColor2 = Color.fromRGBO(98, 116, 180, 0.3);
  static const hintPayment = Color.fromRGBO(0, 0, 0, 0.85);
  static const hintMessgePayment = Color.fromRGBO(0, 0, 0, 0.6);
  static const hintdiviPayment = Color.fromRGBO(0, 0, 0, 0.3);
  static const invoiceBackButtonColor = Color.fromRGBO(205, 216, 255, 0.52);
  static const invoiceTextButtonColor = Color.fromRGBO(30, 66, 197, 1);
  static const textFieldBackColor = Color.fromRGBO(237, 241, 255, 1);

  // ONBORDING COLORS
  static const swipeColor = Color.fromRGBO(191, 191, 191, 0.46);
  static const hintSortedColors = Color.fromRGBO(0, 0, 0, 0.66);
  static const backBackProfileColors = Color.fromRGBO(249, 249, 249, 1);
  static const borderProfileFieldColor = Color.fromRGBO(215, 215, 215, 1);
  static const backInfoColor = Color.fromRGBO(234, 234, 234, 1);



}

class AppColorsDark {
  // LOGIN AND REGISTER COLORS
  static const titleColor = Color.fromRGBO(230, 230, 230, 1); // Light gray for titles
  static const primaryButtonColor = Color.fromRGBO(30, 90, 255, 0.9); // Brighter blue for buttons
  static const buttonColor = Color.fromRGBO(60, 80, 120, 0.5); // Darker blue-gray for secondary buttons
  static const primaryButtonTextColor = Color.fromRGBO(230, 230, 230, 1); // Light text for buttons
  static const buttonTextColor = Color.fromRGBO(150, 180, 255, 1); // Light blue for button text
  static const hintColor = Color.fromRGBO(150, 150, 150, 0.7); // Medium gray for hints
  static const dividerColor = Color.fromRGBO(80, 80, 80, 0.4); // Subtle gray for dividers
  static const dragColor = Color.fromRGBO(100, 100, 100, 1); // Darker gray for drag
  static const dragColor1 = Color.fromRGBO(80, 80, 80, 1); // Slightly darker for drag
  static const lightBackgroundColor = Color.fromRGBO(40, 40, 40, 1); // Dark gray background
  static const backgroundColor = Color.fromRGBO(30, 30, 30, 1); // Darker background
  static const linkLowColor = Color.fromRGBO(100, 140, 255, 1); // Light blue for links
  static const linkColor = Color.fromRGBO(70, 110, 255, 1); // Brighter blue for links
  static const orColor = Color.fromRGBO(120, 120, 120, 0.5); // Gray for "or" text
  static const iconBackground = Color.fromRGBO(60, 60, 60, 1); // Dark gray for icon backgrounds

  // HOME COLORS
  static const homeBackgroundColor = Color.fromRGBO(25, 25, 25, 1); // Very dark gray for home
  static const docHomeBackgroundColor = Color.fromRGBO(50, 50, 50, 0.5); // Semi-transparent dark gray
  static const homeMainColor = Color.fromRGBO(200, 200, 200, 1); // Light gray for main text

  // EXPLORE COLORS
  static const categoryTextColor = Color.fromRGBO(180, 180, 180, 1); // Light gray for category text
  static const categoryBackgroundColor = Color.fromRGBO(50, 50, 50, 0.3); // Dark gray for category background
  static const categoryBorderColor = Color.fromRGBO(100, 100, 100, 0.5); // Gray for borders

  // APPOINTMENTS COLORS
  static const hourAvailableColor = Color.fromRGBO(80, 100, 120, 1); // Dark blue-gray for available hours
  static const alseAvailableColor = Color.fromRGBO(60, 60, 60, 0.7); // Darker gray for alternatives
  static const hourNotAvailableColor = Color.fromRGBO(80, 100, 120, 0.4); // Faded for unavailable
  static const arrowBackgroundColor = Color.fromRGBO(70, 70, 70, 1); // Dark gray for arrows

  // FILTER COLORS
  static const removeColor = Color.fromRGBO(255, 80, 80, 1); // Bright red for remove actions
  static const messageBackColor = Color.fromRGBO(40, 40, 40, 1); // Dark gray for message backgrounds

  // CONSULT COLORS
  static const pinColor = Color.fromRGBO(100, 120, 160, 0.5); // Blue-gray for pins

  // CALL COLORS
  static const someRedColor = Color.fromRGBO(255, 100, 100, 1); // Bright red for calls
  static const nonColor = Colors.transparent; // Transparent remains unchanged

  // SETTINGS COLORS
  static const backBackColor = Color.fromRGBO(50, 50, 50, 0.7); // Dark gray for backgrounds
  static const settingTitleColor = Color.fromRGBO(180, 180, 180, 0.8); // Light gray for titles
  static const hintSettingColors = Color.fromRGBO(140, 140, 140, 0.7); // Medium gray for hints
  static const logSettingColor = Color.fromRGBO(255, 80, 80, 1); // Bright red for log out

  // PAYMENT COLORS
  static const paymentBackColor1 = Color.fromRGBO(100, 80, 140, 0.4); // Dark purple for payment
  static const paymentBackColor2 = Color.fromRGBO(80, 90, 140, 0.35); // Dark blue for payment
  static const hintPayment = Color.fromRGBO(200, 200, 200, 0.9); // Light gray for payment hints
  static const hintMessgePayment = Color.fromRGBO(150, 150, 150, 0.7); // Medium gray for messages
  static const hintdiviPayment = Color.fromRGBO(100, 100, 100, 0.4); // Gray for dividers
  static const invoiceBackButtonColor = Color.fromRGBO(60, 80, 120, 0.6); // Dark blue for invoice buttons
  static const invoiceTextButtonColor = Color.fromRGBO(150, 180, 255, 1); // Light blue for text
  static const textFieldBackColor = Color.fromRGBO(50, 50, 50, 1); // Dark gray for text fields

  // ONBOARDING COLORS
  static const swipeColor = Color.fromRGBO(100, 100, 100, 0.5); // Gray for swipe indicators
  static const hintSortedColors = Color.fromRGBO(150, 150, 150, 0.8); // Medium gray for hints
  static const backBackProfileColors = Color.fromRGBO(30, 30, 30, 1); // Dark gray for profiles
  static const borderProfileFieldColor = Color.fromRGBO(80, 80, 80, 1); // Dark gray for borders
  static const backInfoColor = Color.fromRGBO(45, 45, 45, 1); // Dark gray for info backgrounds
}